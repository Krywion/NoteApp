services:
  noteApp:
    container_name: noteapp
    build: .
    image: noteapp:v1
    ports:
      - 8050:80
    expose:
      - 80
    networks:
      - internalnet

networks:
    internalnet:
        driver: bridge

